import XCTest
@testable import BunProKit

final class BunProKitTests: XCTestCase {
    func testExample() {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
//        XCTAssertEqual(BunProKit().text, "Hello, World!")
    }

    static var allTests = [
        ("testExample", testExample),
    ]
}
